name: eleaguehub
description: EleagueHub MVP skeleton (Android only)
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State + DI
  flutter_riverpod: ^2.5.1

  # Navigation
  go_router: ^14.2.7

  # Persistence
  shared_preferences: ^2.2.3

  # Nice to have
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
